# Dependencias de ContaFlow (Python 3.11+)
fastapi>=0.110
uvicorn>=0.27
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
openpyxl>=3.1
reportlab>=4.0
pillow>=10.0

# Ventana de escritorio nativa. En Windows incrusta WebView2 (el motor de
# Edge, ya incluido en Windows 10/11). Si falta, la aplicación abre el
# navegador en modo aplicación como alternativa.
pywebview>=5.0; sys_platform == "win32"
